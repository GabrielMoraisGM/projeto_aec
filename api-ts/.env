# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

# Server configuration
PORT=3000

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_aec
DB_USER=application_aec
DB_PASSWORD=123456

#token
SECRET=mywebtoken

#Frontend config
LPORT_FRONT=5173